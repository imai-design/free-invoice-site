# SPEC: 無料請求書・見積書メーカー MVP

## 位置づけ
「無料ホームページの会社」give品目の第2弾（自己完結型ツール枠）。無料HP製作所と同じデザイン言語・同じ「無料でずっと無料」の思想。世界中どの個人事業主・フリーランスにも必要な書類なので、汎用性が最も高い。

## 目的（1行）
質問に答えるだけで、その場で請求書・見積書がブラウザ内で完成し、印刷またはPDF保存できる無料ツール。AI呼び出し不要（サーバー通信ゼロ）＝原価は完全にゼロ。

## 成果物（このリポジトリ直下）
- `index.html` — LP（無料HP製作所のindex.htmlと対になる構成）
- `make.html` — 作成ページ（フォーム→リアルタイムプレビュー→印刷/PDF）
- `assets/style.css` — 共有スタイル
- `assets/app.js` — make.htmlのロジック（vanilla JS。外部通信・fetch一切禁止）

## 技術制約（絶対厳守）
- vanilla HTML/CSS/JSのみ。ライブラリ・ビルド・バックエンド・APIコールは一切禁止（完全クライアントサイド）
- **外部への通信は一切発生させない**（フォーム入力はブラウザ内で完結、サーバーに送信しない。プライバシー上の要求でもある）
- 日本語。絵文字は一切使わない
- レスポンシブ必須（375px幅で崩れない）
- キーボードフォーカス可視、`prefers-reduced-motion`尊重
- 印刷時（`@media print`）はA4サイズで綺麗に収まるレイアウトにすること
- Google Fonts: `Zen Kaku Gothic New`（400/500/700/900）+ `IBM Plex Mono`（400/500）※無料HP製作所と統一

## デザイントークン（無料HP製作所と統一。厳守）
```css
--paper: #FBFAF6;
--ink: #1E2A44;
--ink-deep: #141D31;
--yamabuki: #F5B517;
--yamabuki-deep: #D99A00;
--line: #E5E1D8;
--white: #FFFFFF;
```
- 雰囲気は無料HP製作所と統一（町の店の看板・信頼感）
- 請求書プレビュー自体は「実際の請求書らしい」白背景・黒文字ベースの実務的な見た目にする（装飾は最小限、印刷して恥ずかしくないもの）

## index.html（LP）構成

1. **ヘッダー**: 左「RYOSEIWORLD」ワードマーク＋「無料請求書メーカー」、右にCTA「無料で作る」→ make.html
2. **ヒーロー**: 特大見出し「請求書も、無料で作れます。」サブコピー「登録も、会員登録もいりません。今すぐ、その場で作れます。」CTA「いますぐ作る」
3. **なぜ無料？**: 「これは、通信すら発生しません。あなたのブラウザの中だけで完結するので、原価はゼロです。データも、どこにも送られません。」
4. **3ステップ**: 入力する（宛先・品目・金額）／確認する（その場でプレビュー）／保存する（印刷またはPDFで保存）
5. **最後のCTA**: 「まずは、1枚作ってみませんか。」
6. **フッター**: RYOSEIWORLD／「みんなが欲しいものを、無料で作ってあげる会社」（無料HP製作所と統一）／無料HP製作所への相互リンク「ホームページも無料で作れます →」

## make.html（作成ページ）構成

2カラム（左=入力フォーム、右=プレビュー。375pxはプレビューが先）

**左フォーム**（vanilla JS、全てブラウザ内で完結）:
1. 書類の種類（radio: 請求書／見積書）
2. 発行者情報: 屋号・氏名（text）、住所（text, 任意）、電話（tel, 任意）、メール（email, 任意）
3. 宛先: 会社名・氏名（text）
4. 発行日（date, 初期値=今日）
5. 品目リスト（動的追加可能な行。各行=品名(text)・数量(number)・単価(number)。「＋品目を追加」ボタンで行を増やせる。最低1行）
6. 消費税（radio: 10%／8%／なし。デフォルト10%）
7. 備考（textarea, 任意。例：振込先など）

**右プレビュー**（入力に即時反映、実際の請求書見本の体裁）:
- 発行者情報／宛先／発行日／書類番号（自動採番: 発行日から生成、例 `INV-20260803-001`のような形式でよい）
- 品目テーブル（品名・数量・単価・小計）
- 小計・消費税・合計を自動計算
- 備考欄

**下部ボタン**:
- 「印刷する」ボタン → `window.print()`。`@media print`で入力フォーム側を`display:none`にし、プレビューだけがA4で印刷される
- 「PDFとして保存」の案内文（「印刷ボタンから『PDFに保存』を選ぶと、PDFとして保存できます」という説明。実装としては印刷ダイアログに任せる。外部ライブラリでのPDF生成はしない）

## コピーの掟
- 主語を「AI」にしない
- 「作ります」「できあがります」と人の言葉で
- 専門用語（インボイス制度の詳細など）を前面に出しすぎない。ただしインボイス登録番号の入力欄は発行者情報に任意で1つ用意してよい（`（任意）インボイス登録番号`）

## 完了条件
- index.html / make.html ともコンソールエラーなし
- make.html: 品目を複数行追加でき、金額がリアルタイムで正しく計算される（数量×単価の合計、消費税、総合計）
- 印刷プレビュー（`@media print`）でフォーム部分が非表示になり、請求書部分だけがA4で綺麗に収まる
- 375px幅で崩れない
- **外部への通信が一切発生しない**（Network タブで確認可能な形。Google Fontsの読み込みだけは許容）

## 絶対に超えてはいけない範囲（今回明記）
- **このタスクでgit commit / git push を行わないこと**。ファイルの作成・編集のみ行う
- **指定されたファイル（index.html, make.html, assets/style.css, assets/app.js）以外のディレクトリ・ファイルを作らないこと**。特に `worker/` や `server/`、`api/`、`.ts`ファイル、`package.json`、バックエンド関連の一切を作らないこと
- フォーム送信・API呼び出し・fetch・XMLHttpRequestを一切書かないこと。このツールは完全にクライアントサイドで完結する
- 完了後、変更ファイル一覧と動作確認した点を報告すること。それ以上の機能を自己判断で追加しないこと

## 進め方
完成後、SPEC.mdに書かれている範囲内で完結していることを自己確認し、報告すること。
